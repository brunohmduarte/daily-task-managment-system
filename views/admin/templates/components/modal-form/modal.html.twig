{% block button_modal_form %}
    <a href="#" class="btn btn-danger mb-2 ms-1" data-bs-toggle="modal" data-bs-target="#record-del-alert-modal">
        <i class="mdi mdi-18px mdi-delete"></i>
        <b>Deletar</b>
    </a>
{% endblock %}


{% block dialog_modal_form %}
    <div id="record-del-alert-modal" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-dialog modal-sm">
            <div class="modal-content modal-filled bg-danger">
                <div class="modal-body p-4">
                    <div class="text-center">
                        <i class="ri-close-circle-line h1"></i>
                        <h4 class="mt-2">Atenção!</h4>
                        <p class="mt-3">Deseja realmente excluír esse registro?</p>
                        <p>Uma vez confirmada a exlusão desse registro, o mesmo não poderá ser recuperado futuramente.</p>
                        <button type="button" class="btn btn-light my-2" id="btn-del" data-bs-dismiss="modal" data-link="{{ LINK }}">
                            <b>Sim, eu quero excluir!</b>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}


{% block js_modal_form %}
    <script>
        var btnConfirm = document.getElementById('btn-del'),
            link = btnConfirm.getAttribute('data-link');

        btnConfirm.addEventListener('click', (e) => {
            window.location.href = link;
        });
    </script>
{% endblock %}